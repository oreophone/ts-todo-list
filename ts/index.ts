// To-do list
// Using a JSON database built from custom object types, allowing users to:
// * Add/Check/Assign Priority to TODO's
// * View/Delete previously completed TODO's
// + Choose between >=2 themes

// TODO
// - Write funcion templates, types

import {TodoList} from "./TodoList";

function main(): void {
    var todo = new TodoList();
}

main()