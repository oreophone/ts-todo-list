{
  "name": "ts-todo",
  "author": "Adrian Carpio",
  "description": "A TODO list, made using vanilla TS, localStorage and webpack. My first TS project!",
  "scripts": {
    "build": "tsc && webpack",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc-watch --onSuccess 'webpack'"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "webpack": "^5.92.1",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "onchange": "^7.1.0",
    "tsc-watch": "^6.2.0"
  }
}
